apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thingsboard-component.fullname" . }}-test-connection"
  labels:
    {{- include "thingsboard-component.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thingsboard-component.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
